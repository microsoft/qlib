{
  "name": "qlib-ci",
  "private": true,
  "description": "Locked tooling for Qlib's pull request title check",
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2"
  }
}
